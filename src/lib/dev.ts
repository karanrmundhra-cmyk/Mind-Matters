/**
 * Temporary dev defaults used until authentication (Step 7) provides the real
 * user/space/timezone. Centralised so there is ONE place to replace when auth lands.
 * Client-safe (no server imports) so both UI and server code can import it.
 */
export const DEV_TZ = 'Asia/Kolkata';
